//Rendering search Results on searchResult Page

const TMDB_API_KEY = "d94dfa549a03679b14c6609bddc9a3b0";

function getQuery() {
  //Get search query from location.search
  //return fetchable url for TMDB Search API with query given and API key
}

function fetchSearch(url) {
  //fetching data from url
  //Once data obtain call renderSearcn(data)
  //catch Error
}

function renderSearch(data) {
  //Render #searchResultCount and #searchResultContainer with data given
  //EveryResultBox will have a href link to (moviepage.html?id={id}&title={TITLE}) to a new tab
  //Result box will have data-title, data-id, data-poster_url
}
